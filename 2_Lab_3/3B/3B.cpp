#include "Function_Header.h"

int main()
{
	setlocale(LC_ALL, "RU");
	int array[N];
	int B[N], C[N],D[N];
	int a, b, c, size;

	std::cout << "Введите размер массива: ";
	std::cin >> size;
	if (size<0 || size==0) {
		std::cout << "Вы ввели не верное значение";
	}


}